
// Definition: A binary tree where the left child node is less than the parent, and the right child node is greater.

// Example: Efficient searching, sorting, and range-based queries.

// Use Case: BSTs are useful for scenarios that require efficient searching, sorting, and range-based queries on ordered data.

// Code Example:


let bst = new BinarySearchTree();
bst.insert(5);
bst.insert(3);
console.log(bst.search(3)); 
// Output: true