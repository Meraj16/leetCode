// Definition: Collections of unique values, where each value occurs only once.

// Example: Storing a unique list of items.

// Use Case: Sets are useful when you need to eliminate duplicate values and quickly check for membership.

// Code Example:

let set = new Set();
set.add(1);
set.add(2);
console.log(set.has(1)); // Output: true